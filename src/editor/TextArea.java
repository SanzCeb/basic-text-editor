package editor;

import javax.swing.*;
import java.awt.*;

public class TextArea extends JTextArea {
    public TextArea() {
        setSize(new Dimension(200, 200));
        setName("TextArea");
    }
}
